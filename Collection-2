      static void Main(string[] args)
      {
         List<int> numbers = new List<int>();
         int[] tempMassive;
         bool isWork = true;

         while (isWork)
         {
            Console.Clear();
            Console.ForegroundColor = ConsoleColor.Blue;
            Console.BackgroundColor = ConsoleColor.White;
            Console.WriteLine("\n данные по массиву ");

            Console.ForegroundColor = ConsoleColor.White;
            Console.BackgroundColor = ConsoleColor.Black;
            Console.Write("размер массива: ");
            Console.ForegroundColor = ConsoleColor.DarkYellow;
            Console.WriteLine(numbers.Count);

            Console.ForegroundColor = ConsoleColor.White;
            Console.Write("элементы массива: ");
            Console.ForegroundColor = ConsoleColor.Yellow;

            foreach (int number in numbers)
            {
               Console.Write(number + " | ");
            }

            Console.ForegroundColor = ConsoleColor.White;
            Console.WriteLine("\n\n(1) - задать массив чисел\n(2) - добавить число\n(3) - удалить число\n" +
                              "(4) - вывод суммы чисел\n(exit) - выход\n");

            Console.Write("введите команду: ");
            string select = Console.ReadLine();
            
            switch (select)
            {
               case "1":
                  numbers.Clear();
                  numbers.AddRange(AddMassive(out tempMassive));
                  break;

               case "2":
                  Console.Write("Введите целое число, чтобы добавить в массив: ");
                  numbers.Add(ReadInteger());
                  break;

               case "3":
                  Console.Write("Введите число, которое следует удалить: ");
                  bool isDelete = numbers.Remove(ReadInteger());

                  if (!isDelete)
                  {
                     Console.WriteLine("нет такого числа в списке");
                     Console.ReadKey();
                  }
                  break;

               case "4":
                  CalculationSumm(numbers);
                  Console.ReadKey();
                  break;

               case "exit":
                  isWork = false;
                  break;

               default:
                  Console.WriteLine("!!! неверная команда !!!");
                  Console.ReadKey();
                  break;
            }
         }
      }

      static int[] AddMassive(out int[] tempMassive)
      {
         Console.Write("Введите количество элементов: ");
         tempMassive = new int[ReadInteger()];

         for (int i = 0; i < tempMassive.Length; i++)
         {
            Console.Write($"введите {i+1} число: ");;
            tempMassive[i] = ReadInteger();
         }
         return tempMassive;
      }

      static int ReadInteger()
      {
         string input;
         int number = 0;
         bool isNumber = false;

         while (isNumber == false)
         {
            input = Console.ReadLine();
            isNumber = int.TryParse(input, out number);

            if (!isNumber)
            {
               Console.WriteLine("Вы ввели некорректные данные.");
               Console.Write("Введите целое число: ");
            }
         }
         return number;
      }

      static List<int> CalculationSumm(List<int> numbers)
      {
         int summ = 0;
         foreach (int item in numbers)
         {
            summ += item;
         }

         Console.ForegroundColor = ConsoleColor.Yellow;
         Console.WriteLine("\nсумма чисел: " + summ);
         Console.ForegroundColor = ConsoleColor.White;
         return numbers;
      }
